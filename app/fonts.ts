import { Inter, Noto_Sans_KR as NotoSansKR } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin', 'vietnamese'],
  variable: '--font-sans',
  weight: 'variable',
});

export const notoSansKR = NotoSansKR({
  subsets: ['latin', 'vietnamese'],
  variable: '--font-sans',
  weight: 'variable',
});
